# Getting Started

## Build the manual

The folder index becomes a section divider. Child headings inherit their effective level from the
folder hierarchy.

| Input | Output | Purpose |
| --- | --- | --- |
| Markdown | HTML | Searchable offline documentation |
| Markdown | PDF | A4 print and distribution document |

> [!danger] Example placeholder
> Replace this callout with a real diagram or screenshot before publishing an operator manual.

### Use a render hook

The example hook renders `[RUN]` as a semantic key component shared by HTML and PDF.
